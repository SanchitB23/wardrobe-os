/**
 * Outfit generation domain — builds new outfits from wardrobe items, scored
 * with the rule-based outfit engine and each item's StyleDNA.
 */

export * from "@/domain/generation/OutfitGenerationEngine";
